\chapter{Analysis} % Potentially talk about uncertainty here or in the methods chapter

    After getting the diffractogram the ranges for the amorphous hump, and most intense crystalline peak where taken as input in a text file. 
    This text file was then read by the python program (linked in appendix \ref{chap: data code}) developed to determine composition, and depth. 
    The composition was determined by the methods in the previous chapter. 
    The depth was determined by mixing the indices of refraction of the materials in the sample (phosphorous and silicon) using the composition percent as the weights. 
    Indices of refraction were used from The Center For X-Ray Optics (\href{https://henke.lbl.gov/optical_constants/getdb2.html}{linked here})\cite{refractionIndex}. 
    The above was combined by using the following equation:
    \begin{equation}
        n=m_a n_a+m_c n_c
        \label{eq: index mixing}
    \end{equation}
    Where $n$ is the layer's index of refraction, $n_a$ and $n_c$ are the index of refraction for phosphorous and silicon respectively.
    The quantities $m_a$ and $m_c$ are the mixture fraction of the phosphorous and silicon respectively.

    This new index of refraction for the layer was then converted into an attenuation coefficient by the following:
    \begin{equation}
        \alpha=\frac{4\pi n_I}{\lambda}
        \label{eq: attenuation coefficient}
    \end{equation}
    Where $n_I$ is the imaginary component of the layer's index of refraction, and $\lambda$ is the wavelength\cite{hecht}. 
    The inverse of the attenuation coefficient is then the skin depth of the material, or the depth at which the x-rays have attenuated by a third. 
    This skin depth was then applied until 90\% attenuation was achieved. 
    The vertical component of the path length was then taken to be depth from the surface of the material (as shown below in figure \ref{fig:path length to depth}). 

    \begin{figure} [H]
        \centering
        \begin{tikzpicture}

        % --- 1. Colors and Styles ---
        \definecolor{sampleGrey}{RGB}{140, 160, 190}
        \definecolor{rayGreen}{RGB}{0, 100, 0} 
            
        \tikzset{
            sampleLayer/.style={fill=sampleGrey, draw=none},
            incidentRay/.style={line width=2.5pt, rayGreen, line cap=round},
            refLine/.style={dashed, gray, thin},
            dimLine/.style={draw=black, <->, thin}
        }

        % --- 2. Manual Layer Placement (Uniform Grey) ---
        \foreach \y in {0, 1, 2, 3, 4} {
            \fill[sampleLayer] (-5, \y) rectangle (5, \y + 0.3);
        }

        % --- 3. Coordinates ---
        % Surface level (top of the topmost layer)
        \coordinate (Surface) at (0, 4.3);
            
        % Entry point where ray hits the surface
        \coordinate (P_entry) at (-3.5, 4.3);
            
        % End point deep in the sample
        \coordinate (P_end) at (-1, 1.3);

        % Start point of the ray
        \coordinate (Inc_start) at (-5.5, 6.5);

        % Horizontal reference point to the RIGHT of the entry point
        \coordinate (H_right) at (-1.5, 4.3);

        % --- 4. Drawing Geometry ---
        % The ray going into the sample
        \draw[incidentRay] (Inc_start) -- (P_end);

        % Vertical line from surface to the end point
        \draw[dimLine] (-1, 4.3) -- (P_end) node[midway, right] {$y$};
            
        % Dashed line to show the surface plane for the angle
        % \draw[refLine] (P_entry) -- (H_left);

        % --- 5. Angle Omega (Measured the "other way") ---
        % This measures the glancing angle toward the interior of the V
        \pic [draw, "$\omega$", angle radius=1.2cm, angle eccentricity=1.3] {angle = Inc_start--P_entry--H_left};

    \end{tikzpicture}
    \caption{Path length to depth into the sample}
    \label{fig:path length to depth}
    \end{figure}

    Results from the methods are given in the figure below:

    \begin{figure}[H]
        \centering
        \includesvg[width=1\linewidth]{Pictures/Percentage vs Depth (inset).svg}
        \caption{Percentage of amorphous vs depth for the three methods}
        \label{fig:line graph amorphous with depth}
    \end{figure}

    % \begin{figure}[H]
    %     \centering
    %     \includesvg[width=1\linewidth]{Pictures/Percentages with Uncertainty.svg}
    %     \caption{Percentage of amorphous vs omega for the three methods with fractional uncertainty}
    %     \label{fig:line graph amorphous with errorbars}
    % \end{figure}

    From figure, \ref{fig:line graph amorphous with depth} we can see that the intensity method is not in agreement with the curve fit or summation methods.
    The curve fit, and summation methods are expected to be in agreement with each other since the methods are both integral methods. 
    It should be noted that the three methods seem to give a value greater than unity for the lowest depth, but this is an artifact of forcing the program to find data that is not there. 
    At the omega angle that corresponds to this low depth, the crystalline peaks do not appear. 
    This means only an amorphous hump is available for the data analysis. 
    